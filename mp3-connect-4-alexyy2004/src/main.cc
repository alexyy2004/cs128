#include <iostream>

#include "board.hpp"

int main() {
  /*
  Board student;
  DropDiskToBoard(student,DiskType::kPlayer1,1);
  DropDiskToBoard(student,DiskType::kPlayer1,1);
  DropDiskToBoard(student,DiskType::kPlayer1,1);
  DropDiskToBoard(student,DiskType::kPlayer1,1);
  std::cout << SearchForWinner(student,DiskType::kPlayer1,WinningDirection::kVertical) << std::endl;
  std::cout << CheckForWinner(student,DiskType::kPlayer1) << std::endl;
  std::cout << BoardLocationInBounds(Board::kBoardHeight,Board::kBoardWidth) << std::endl;
  */
}